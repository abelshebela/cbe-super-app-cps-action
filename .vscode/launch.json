{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cps-action",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd",
            "cwd": "${workspaceFolder}",
        }
    ]
}
